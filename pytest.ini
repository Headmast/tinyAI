[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
    api: marks tests that require a real API key (deselect with '-m "not api"')
