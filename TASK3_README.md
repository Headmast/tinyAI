# Task 3: Миграция на облачный провайдер Cloud.ru

## Описание задания

В этом задании выполнена миграция утилиты с OpenAI API на облачный провайдер **Cloud.ru Foundation Models API**.

## Ключевые изменения

### 1. Изменение провайдера API

**Было (OpenAI):**
```python
from openai import OpenAI

api_key = os.getenv("OPENAI_API_KEY")
client = OpenAI(api_key=api_key)
```

**Стало (Cloud.ru):**
```python
from openai import OpenAI

api_key = os.getenv("CLOUD_API_KEY")
base_url = "https://foundation-models.api.cloud.ru/v1"
client = OpenAI(api_key=api_key, base_url=base_url)
```

### 2. Изменение модели

**Было:** 7 моделей OpenAI (gpt-3.5-turbo, gpt-4o-mini, gpt-4o, gpt-4-turbo, gpt-4, gpt-5.4, gpt-5-nano)

**Стало:** 1 модель Cloud.ru
- `zai-org/GLM-4.7-Flash` - облачная модель GLM-4.7-Flash

### 3. Изменение расчета стоимости

GLM-4.7-Flash предоставляется **бесплатно**:
```python
"prompt_price": 0.0,
"completion_price": 0.0
```

## Настройка

### Переменные окружения

Создайте файл `.env` с ключом API:

```bash
CLOUD_API_KEY=your_cloud_ru_api_key_here
```

⚠️ **Важно**: Используется `CLOUD_API_KEY` вместо `OPENAI_API_KEY`

## Использование

### Базовое использование

```bash
python3 llm_cli.py
```

### Доступные команды

Все команды из предыдущих версий сохранены:

- `quit` / `exit` / `q` - выход
- `compare` - сравнение всех режимов
- `modes` - показать список режимов
- `mode N` - переключиться на режим N (1-6)
- `models` - показать список моделей
- `model <name>` - переключиться на модель (в данной версии только одна модель)

### Режимы работы

Все 6 режимов работы сохранены:

1. **Без ограничений** - стандартный запрос
2. **С явным форматом ответа** - структурированный текст
3. **С ограничением длины** - max_completion_tokens=100
4. **С явной инструкцией завершения** - маркер [КОНЕЦ]
5. **С выводом в формате JSON** - JSON с полями answer, summary, key_points
6. **С метапромптингом** - двухэтапный процесс с автоопределением роли

## Примеры использования

### Пример 1: Простой запрос

```bash
python3 llm_cli.py

You: Объясни квантовую физику
Assistant: [Ответ от GLM-4.7-Flash]
[Режим: Без ограничений | Токены: 150 | Слова: 85 | Стоимость: $0.000000]
```

### Пример 2: Режим метапромптинга

```bash
You: mode 6
✓ Переключено на режим 6: С метапромптингом (двухэтапный)

You: Как улучшить производительность веб-приложения?
[МЕТААНАЛИЗ]
РОЛЬ: Веб-разработчик / Performance Engineer
ОБЛАСТЬ: Веб-разработка, оптимизация производительности
НАВЫКИ: Профилирование, кеширование, CDN, минификация, lazy loading

[ОТВЕТ]
[Детальный ответ с учетом определенной роли и навыков]
```

### Пример 3: Сравнение режимов

```bash
You: compare
Введите запрос для сравнения: Что такое Docker?

# Система покажет ответы всех 6 режимов
# С маркерами [Конец ответа] после каждого
```

## Технические детали

### API совместимость

Используется OpenAI-совместимый интерфейс Cloud.ru:
- Та же библиотека `openai`
- Те же методы `client.chat.completions.create()`
- Те же параметры (model, messages, temperature, max_completion_tokens)

### Параметры по умолчанию

```python
{
    "model": "zai-org/GLM-4.7-Flash",
    "temperature": 0.5,
    "max_tokens": 2500,
    "presence_penalty": 0,
    "top_p": 0.95
}
```

### Логирование

Логирование работает так же, как и раньше:
- Папка `logs/` (исключена из git)
- Формат: `conversation_YYYYMMDD_HHMMSS.json`
- Трекинг токенов и слов
- Метаданные о режиме и модели

## Тестирование

Все тесты обновлены под новый провайдер:

```bash
python3 -m pytest test_llm_cli.py -v
```

**Результат:** 23 теста проходят ✅

### Обновленные тесты

- `TestModels`: 4 теста для единственной модели GLM-4.7-Flash
- `TestCostCalculation`: 2 теста для бесплатной модели (cost = 0.0)
- `TestIntegration`: 2 теста с GLM моделью
- Остальные тесты без изменений

## Преимущества миграции

### ✅ Преимущества

1. **Бесплатное использование** - нет расходов на API
2. **Локализация** - серверы Cloud.ru в России
3. **Совместимость** - OpenAI-подобный интерфейс
4. **Все функции сохранены** - 6 режимов работы
5. **Метапромптинг работает** - двухэтапный процесс

### ⚠️ Особенности

1. **Одна модель** - только GLM-4.7-Flash (нет выбора)
2. **Переменная окружения** - CLOUD_API_KEY вместо OPENAI_API_KEY
3. **Базовый URL** - нужно указывать base_url явно

## Структура проекта

```
tinyAI/
├── llm_cli.py              # Основной файл (обновлен для Cloud.ru)
├── test_llm_cli.py         # Тесты (обновлены)
├── .env                    # CLOUD_API_KEY (не в git)
├── .gitignore              # Исключает logs/ и .env
├── logs/                   # Логи взаимодействий
├── TASK3_README.md         # Эта документация
├── TASK2_README.md         # Документация предыдущей версии
└── README.md               # Общая документация
